import { SortHighscoresPipe } from './sort-highscores.pipe';

describe('SortHighscoresPipe', () => {
  it('create an instance', () => {
    const pipe = new SortHighscoresPipe();
    expect(pipe).toBeTruthy();
  });
});
